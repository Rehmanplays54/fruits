import { View, Text } from 'react-native'
import React from 'react'
import { useRoute } from '@react-navigation/native'
import { Image } from 'react-native-animatable';

const OList = () => {

  // console.log(name,image,text1,number);
  return (
    <View>
 
    </View>
  )
}

export default OList